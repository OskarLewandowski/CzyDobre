@model List<CzyDobre.Models.AspNetUser>

@{
    ViewBag.Title = "UserList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="role-div">
    <h2>Użytkownicy</h2>
    <div style="text-align: center;">@Html.Partial("_Notifications")</div>
    <hr />
    <div class="table">
        <table id="users" class="table table-bordered table-hover display">
            <thead>
                <tr class="table-role">
                    <th>ID</th>
                    <th>Email | Potwierdzenie?</th>
                    <th>Zablokowany</th>
                    <th>Imię</th>
                    <th>Nazwisko</th>
                    <th>Nick</th>
                    <th>Akcja</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>

                        @if (item.EmailConfirmed == true)
                        {
                            <td> @item.Email Tak✔️</td>
                        }
                        else
                        {
                            <td>@item.Email Nie❌</td>
                        }

                        @if (item.LockoutEndDateUtc != null)
                        {
                            <td style="color: red;">DO @item.LockoutEndDateUtc</td>
                        }
                        else
                        {
                            <td>Nie</td>
                        }

                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.NickName</td>
                        <td>
                            @Html.ActionLink("Edytuj", "Edit", "User", new { item.Id, item.Email, item.EmailConfirmed, item.PasswordHash, item.SecurityStamp, item.PhoneNumber, item.PhoneNumberConfirmed, item.TwoFactorEnabled, item.LockoutEndDateUtc, item.LockoutEnabled, item.AccessFailedCount, item.UserName, item.FirstName, item.LastName, item.NickName, item.LastBanDays, item.BanComment, item.WhoGaveBan }, new { @class = "btn btn-primary", onclick = "return confirm('Czy na pewno chcesz EDYTOWAĆ użytkownika?');" })
                            <hr />
                            @Html.ActionLink("Usuń", "Delete", "User", new { idUser = item.Id, nick = item.NickName }, new { @class = "btn btn-danger", onclick = "return confirm('Czy na pewno chcesz USUNĄĆ użytkownika?');" })
                            <hr />
                            @if (item.LockoutEndDateUtc == null)
                            {
                                @Html.ActionLink("BAN", "Ban", "User", new { item.Id, item.Email, item.EmailConfirmed, item.PasswordHash, item.SecurityStamp, item.PhoneNumber, item.PhoneNumberConfirmed, item.TwoFactorEnabled, item.LockoutEndDateUtc, item.LockoutEnabled, item.AccessFailedCount, item.UserName, item.FirstName, item.LastName, item.NickName, item.LastBanDays, item.BanComment, item.WhoGaveBan }, new { @class = "btn btn-warning", onclick = "return confirm('Czy na pewno chcesz ZABLOKOWAĆ użytkownika?');" })
                            }
                            else
                            {
                                @Html.ActionLink("UNBAN", "UnBan", "User", new { item.Id, item.Email, item.EmailConfirmed, item.PasswordHash, item.SecurityStamp, item.PhoneNumber, item.PhoneNumberConfirmed, item.TwoFactorEnabled, item.LockoutEndDateUtc, item.LockoutEnabled, item.AccessFailedCount, item.UserName, item.FirstName, item.LastName, item.NickName, item.LastBanDays, item.BanComment, item.WhoGaveBan }, new { @class = "btn btn-secondary", onclick = "return confirm('Czy na pewno chcesz ODBLOKOWAĆ użytkownika?');" })
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            $('#users').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.11.3/i18n/pl.json"
                }
            });
        });
    </script>
}